import { ReactNode } from 'react';
import { TextInputProps } from '../../atoms/TextInput/interface';

export interface TextInputWithIconProps extends TextInputProps {
  icon: ReactNode
  iconClassName?: string
}

export const xyz = '';
