import { IButtonProps } from '../../atoms/Button/intefaces';
import { ITextInputProps } from '../../atoms/TextInput/interfaces';

export interface ITextInputWithButtonProps extends ITextInputProps {
  buttonProps: IButtonProps
}

export const xyz = '';
